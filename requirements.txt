# ============================================================
# HS Education — Requirements
# All packages for both frontend and backend
# ============================================================


# ── FRONTEND (React) ─────────────────────────────────────
# Install command: npm install --legacy-peer-deps
# Run from: frontend/ folder

react@18.2.0
react-dom@18.2.0
react-scripts@5.0.1
ajv@8.17.1


# ── BACKEND (Node.js API) ────────────────────────────────
# Install command: npm install
# Run from: backend/js/ folder

express@4.18.2
pg@8.11.3
helmet@7.1.0
cors@2.8.5
morgan@1.10.0
nodemon@3.0.2          # dev only — excluded in Docker production build


# ── RUNTIME DEPENDENCIES (provided by Docker images) ─────
# node:20-alpine         — Node.js 20 runtime
# nginx:1.25-alpine      — Nginx web server
# postgres:16-alpine     — PostgreSQL database (Northflank addon)


# ── NOTES ────────────────────────────────────────────────
# Frontend packages are built and discarded — only the static
# build output is included in the final Nginx image (~25MB).
# Backend image includes only production Node.js deps (~80MB).
# No Python, pip, or system-level packages required.
